module Sol where

import Lib

solve :: String -> String
solve input = (first, second) |> pairToStr
  where
    -- * this puzzle is based on the halting problem, to solve this
    -- * look at the puzzle input, decompile it and analyze it
    first = ()
    second = ()
